generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

// Ini adalah tabel User yang diminta ketua
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}